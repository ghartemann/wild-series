{% extends 'base.html.twig' %}

{% block title %} - {{ actor.name }}
{% endblock %}

{% block body %}
    <h1>{{ actor.name }}</h1>
    <img src="https://i.pravatar.cc/300" alt="Random picture.">
    <h2>Biographie</h2>
    <p>{{ actor.biography }}</p>
    <h2>A joué dans</h2>
    <ul>
        {% for program in actor.programs %}
            <li>
                <a href="{{ path("program_show", { program: program.id }) }}">{{ program.title }}</a>
            </li>
        {% endfor %}
    </ul>
    <a href="{{ path('actor_index') }}">Retour</a>
{% endblock %}
